import React from 'react';
import Modal from 'react-modal';

const BookModal = (props) => {
    return(
        <div>
            {/* {props.bookNotes.title} */}
            book modal
        </div>
    );
};

export default BookModal;